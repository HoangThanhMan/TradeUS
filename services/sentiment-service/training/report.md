# Distilled Student — Evaluation Report

> Generated by `python -m training.evaluate_distill` on 2026-08-18T15:28:49+00:00.

## ⚠ Read this before quoting any number below

The configured Gemini key returns `Permission denied: Consumer 'api_key:AIza***REDACTED***' has been suspended.`, so **there is no Gemini anywhere in this comparison**:

- The student's training labels came from `mock` — the keyword fallback in `SentimentService._mock_sentiment_analysis`.
- The "pipeline" column below is that same fallback (`mock`, 0/50 articles reached Gemini).

So this is **student-vs-keyword-heuristic**, not student-vs-Gemini. The Sprint 2 definition of done asks for Gemini-vs-human next to student-vs-human; that row cannot be filled until a working key exists. Every script here is ready — restore the key, re-analyse the backlog, re-run T1.3/T1.4, then re-run this.

What *is* legitimate below: the student-vs-human numbers (the student is a real trained model and the labels are real), the paired comparison, and the latency measurements.

## Eval-set isolation

✅ None of the 50 labelled articles appear in the student's 140 training or 36 validation rows. The student-vs-human number below is measured on articles the student has never seen.

## The comparison that matters

Both systems scored the same 50 hand-labelled articles, bucketed with the same thresholds.

| | Student (local) | Pipeline (API path) | Base checkpoint, untrained |
|---|---|---|---|
| Sentiment-bucket accuracy | **58.0%** | **56.0%** | **66.0%** |
| Emotion accuracy | 28.0% | 24.0% | n/a (3-class model) |
| Majority-class baseline | 36.0% | 36.0% | 36.0% |
| Mean latency / article | **41 ms** | 229 ms | ~same as student |
| Marginal cost / 1k articles | **$0** (local CPU) | ~1350 input tokens/article | $0 |

The student is +2.0 points on bucket accuracy against the path it was distilled from, at 5.6x the speed and no marginal cost.

**But that is the wrong comparison to lead with.** The third column is the one that matters: the *untrained* base checkpoint scores 66.0% on the same articles, so the trained student is 8.0 points **worse than doing no training at all**. See below.

### Did the LoRA pass actually do anything?

The base checkpoint (`mrm8488/distilroberta-finetuned-financial-news-sentiment-analysis`) is already a 3-class financial-sentiment classifier, so it can be scored on the same articles with **no adapter and no training at all**. That is the control that makes the student's number interpretable.

Untrained base: **66.0%**. Student: 58.0% — the LoRA pass **cost** 8.0 points against the checkpoint it started from. Training on the current labels actively degraded a working model.

Base prediction spread: {'bearish': 19, 'neutral': 14, 'bullish': 17} · student: {'bearish': 18, 'bullish': 29, 'neutral': 3}

### Paired breakdown

Aggregate accuracies can match while the two systems disagree on most articles, so here is the article-by-article split:

| Outcome | Articles |
|---|---|
| Both right | 18 |
| Only the student right | 11 |
| Only the pipeline right | 10 |
| Both wrong | 11 |
| **Agreement rate** | **58.0%** |

Where they split (first 10):

| Article | Label | Student | Pipeline |
|---|---|---|---|
| Silver nears $1 billion in volume on Hyperliquid as bitcoin remains fr… | neutral | bullish | **neutral** |
| XRP Slips Below $1.90 as Fed Anxiety Overshadows Major DXC Partnership | bearish | **bearish** | bullish |
| Tom Lee Claims Parabolic Gold and Silver Move Is Masking Bullish Bitco… | bullish | neutral | **bullish** |
| Metroblox unveils USDC-backed stablecoin for transport payments | neutral | bullish | **neutral** |
| 'Forgotten Runiverse' on Ethereum Network Ronin Is the Latest Crypto G… | bearish | **bearish** | neutral |
| Exclusive: Michael Saylor Pioneered Crypto Treasury Firms, But This Et… | neutral | bullish | **neutral** |
| Pippin and Pengu Crypto Lift Meme Coin Market: Start Of Meme Frenzy? | neutral | **neutral** | bearish |
| Fidelity to Enter Stablecoin Market With Ethereum-Based 'Digital Dolla… | bullish | **bullish** | neutral |
| Solana welcomes WisdomTree ecosystem in tokenization move | bullish | **bullish** | neutral |
| BNB Chain News: Enso Spikes, BNB Stabilizes, and Institutions Take Not… | bullish | **bullish** | neutral |

## Student vs. teacher (fidelity)

On the 36 held-out `val.jsonl` rows — how closely the student reproduces the `mock` labels it was trained on.

| Metric | Value |
|---|---|
| Spearman (student vs teacher score) | **0.172** |
| MAE (sentiment score) | **0.668** |
| Bucket agreement | 27.8% |
| Emotion agreement | 36.1% |

This is a fidelity measure, not a quality one. A student can match its teacher perfectly and still be wrong about the world — which is why the human-labelled comparison above is the one that counts.

## Student confusion matrix (vs. human labels)

Rows are the human label, columns are the student's call.

| actual \ predicted | bullish | neutral | bearish |
|---|---|---|---|
| **bullish** | 15 | 1 | 2 |
| **neutral** | 12 | 1 | 3 |
| **bearish** | 2 | 1 | 13 |

| Bucket | Precision | Recall | F1 | Support |
|---|---|---|---|---|
| bullish | 51.7% | 83.3% | 0.64 | 18 |
| neutral | 33.3% | 6.2% | 0.11 | 16 |
| bearish | 72.2% | 81.2% | 0.76 | 16 |

Student prediction spread: {'bearish': 18, 'bullish': 29, 'neutral': 3}.

**The student cannot identify neutral articles.** Recall on the neutral bucket is 6.2% — of 16 genuinely neutral articles it catches 1, pushing the rest into bullish or bearish. Its headline accuracy is carried entirely by the easy directional cases.

This matters more than the headline number for the trading use case: a model that never says "no clear signal" manufactures a directional call on every article it sees, which is exactly the behaviour that would feed noise into the BUY/SELL/HOLD signal downstream. Worth fixing with class weighting or a wider neutral band before anything consumes this.

## Latency and cost

| | Student (local CPU) | Pipeline (API path) |
|---|---|---|
| Mean / article | **41 ms** | 229 ms |
| Median / article | 37 ms | 121 ms |
| Batched (16 at a time) | 52 ms/article | n/a |
| Marginal cost | $0 | see below |

Measured on 50 articles, cpu, one article per call to match how the service invokes it.

### Token accounting

| | Tokens |
|---|---|
| Prompt template | 1,289 |
| Mean article | 61 |
| **Mean input / article** | **1350** |

**95.5% of every API call is the prompt template**, not the article. `SENTIMENT_ANALYSIS_PROMPT` is a long instruction block and the Yahoo summaries are ~150 characters, so the platform is paying mostly to re-send the same instructions thousands of times. Prompt caching or a shorter template is the cheapest available cost win, independent of the student.

No dollar figure is shown because no price was supplied. Pass `--input-price-per-million <rate>` with the current published rate for your model to have it computed — a hardcoded rate would go stale.

*Counted with the student's RoBERTa BPE tokenizer, not Gemini's. Treat as an approximation good to roughly +/-20%.*

## Caveats

- The 50 human labels are LLM-drafted pending review (`label_source: llm_draft`), so these are agreement rates against a draft reference, not verified ground truth.
- 50 articles is a small eval set: a single flipped article moves bucket accuracy by 2 points, so differences under ~5 points are noise.
- The pipeline latency includes a failed Gemini round-trip before falling back, so it measures the current broken path, not a healthy API call.
- Student latency is measured on CPU with no ONNX export or quantisation — it is a floor, not an optimised number.
- Article bodies are Yahoo Finance summaries (~150 characters), not full text.

## Reproducing

```bash
cd services/sentiment-service
python -m eval.evaluate            # pipeline vs human
python -m training.evaluate_distill  # student vs both
```
