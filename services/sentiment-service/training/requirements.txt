# Extra dependencies for the offline distillation training run.
# The service itself does not need these at runtime.
-r ../requirements.txt

torch>=2.1.0
transformers>=4.36.0
peft>=0.10.0
numpy>=1.26.0
matplotlib>=3.8.0
pymongo>=4.6.0
