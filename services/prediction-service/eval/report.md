# LSTM Prediction Model — Evaluation Report

> Generated by `python -m app.ml.evaluate` on 2026-08-18T15:29:48+00:00.

## Setup

- **Checkpoint:** `services/prediction-service/models/crypto_predictor_btcusdt.pt`
- **Symbol / interval:** BTCUSDT / 1h
- **Features:** log_ret, volume_log, sentiment, rsi, macd, bb_width
- **Window size:** 24 bars
- **Split:** first 80% train / last 20% test, chronological (same split as `notebook/train_v3.ipynb`)
- **Test window:** 2024-02-17 20:00:00 → 2024-09-12 22:00:00 (4,995 bars)
- **Test class balance:** 50.9% of bars closed up

**Reconstruction verified.** `train_v3.ipynb` stored a test RMSE of `367.95` USD in the checkpoint; rebuilding the dataset from scratch here reproduces `367.95` USD (Δ 0.0000). Every metric below is therefore measured on the same bars the notebook used.

## Results

| Model | RMSE (log-ret) | MAE (log-ret) | RMSE (USD) | Directional Acc. |
|---|---|---|---|---|
| Naive (0-change) | 0.005874 | 0.003809 | 367.44 | n/a |
| Majority class | 0.005874 | 0.003809 | 367.44 | 50.9% |
| Persistence | 0.008396 | 0.005631 | 524.26 | 48.0% |
| LSTM (ours) | 0.005883 | 0.003820 | 367.95 | 49.1% |
| LSTM (serving-path bb_width) | 0.005884 | 0.003822 | 368.01 | 49.3% |

*The majority-class row borrows the zero-baseline's error columns: it is a directional baseline only, so its own RMSE/MAE would be meaningless.*

## Verdict

The LSTM calls direction correctly on **49.1%** of test bars, which does **not** beat the majority-class floor (50.9%). On this data the model has no demonstrated directional edge — an honest negative result, and exactly what this harness exists to find.

- RMSE on log returns is not lower than the zero-change baseline (0.005883 vs 0.005874).
- A low RMSE against the zero baseline is easy to reach by predicting near-zero returns and says little on its own. Directional accuracy is the metric the BUY/SELL/HOLD signal actually depends on.

## Train/serve parity check

**Mismatch found.** `bb_width` reaches the model on a different scale in the live service than it did in training:

- Training (`ta.BollingerBands.bollinger_wband`, a percentage): `4.269930`
- Serving (`TechnicalIndicatorCalculator.calculate_bollinger_width`, a fraction): `0.042699`
- Ratio: **100.0x**

The `LSTM (serving-path bb_width)` row re-scores the same checkpoint on the same windows with only that feature rescaled to what production supplies. The gap between it and `LSTM (ours)` is what the service is losing to the bug.

## Caveats

- The scalers come from the checkpoint, which fitted them on the full series before splitting (as the notebook did). That leaks test-window scale statistics into training. It flatters the numbers slightly and should be fixed on the next retrain.
- Training windows end one bar before the bar whose return is predicted, while the live service builds its window right up to the latest candle. That one-bar offset is a second train/serve difference worth closing.
- Sentiment comes from a static 2021-2024 CSV, not from the live sentiment-service, so this says nothing about how the deployed sentiment feature behaves.
- Fees and slippage are ignored. Directional accuracy above the baseline is not the same thing as a profitable strategy.

## Reproducing

```bash
cd services/prediction-service
pip install -r eval/requirements.txt
python -m app.ml.evaluate
```
